version: "3.10"

services:
  db:
    image: postgres:latest
    restart: always
    env_file:
      - .env

    volumes:
      - "./postgres:/var/lib/postgresql/data"

  app:
    build: .
    volumes:
      - .:/core
      - ./apps/static:/app/static
      - ./apps/media:/app/media
    ports:
    - "8014:8014"
    command: bash -c "python manage.py collectstatic --no-input && python manage.py makemigrations && python manage.py migrate && uvicorn core.asgi:application --host 0.0.0.0 --port 8014 --workers 4"

    env_file:
      - .env
    depends_on:
      - db
